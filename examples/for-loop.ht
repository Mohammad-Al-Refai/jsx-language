<App>
    </Let id={"sum"} value={0}>
    <For var={"i"} from={0} to={100}>
        <If condition={sum 20 ==}>
            </Break>
        </If>
         </Set id={"sum"} to={i}>
    </For>
    </Print value={sum}>
</App>